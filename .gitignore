# Cache de Python (se regenera solo)
__pycache__/
*.pyc

# Temporales de Office / sistema
~$*
*.tmp
Thumbs.db
.DS_Store

# Ajustes locales de Claude Code (no se comparten)
.claude/settings.local.json

# Documentacion interna: queda solo en la PC, no se publica
Historial/

# Copia local del Gestor de precios: es el master de costos y margenes
# de toda la empresa. El repo es publico, asi que NO se sube.
scripts/costos_gestor.xlsx

# Servidor local para mirar previews (no es del proyecto)
.claude/launch.json
